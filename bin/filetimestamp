#/bin/sh
ls -l $1 | while read line
do
    filename=`echo $line| awk '{print $9}'`
    #echo $filename
    timestamp=`date +%s -r $1/$filename`
    #echo $timestamp
    echo $line| awk  '{print $1 " " $2 " " $3 " " $4 " "$5" "'$timestamp'"  "$9}'
done